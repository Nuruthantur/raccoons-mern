import { IconMap } from "./Item";
import { RandomIcons } from "./Menus";

const IconsContainer = () => {
  return (
    <div>
      <IconMap Links={RandomIcons} title="Icons" />
    </div>
  );
};

export default IconsContainer;
